name: Release Local Agent

# Se activa cuando tag con el formato
on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: windows-latest

    permissions:
      contents: write

    steps:
      # Código del repositorio al tag
      - name: Checkout code
        uses: actions/checkout@v4

      # Entorno de Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Cachea dependencias
      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: local-agent/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('local-agent/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Instala dependencias
      - name: Install dependencies
        run: npm install
        working-directory: ./local-agent

      # Compila
      - name: Build application
        run: npm run build:prod
        working-directory: ./local-agent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SERVER_URL: ${{ secrets.SERVER_URL }}